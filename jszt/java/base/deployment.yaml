apiVersion: apps/v1 
kind: Deployment
metadata:
  name:  demo-app
spec:
  selector:
    matchLabels:
      groups: jszt 
  replicas: 1 
  template:
    metadata:
      labels:
        groups: jszt 
    spec:
      containers:
      - name:  demo-app
        image: harbor.vankeservice.com/demo/demo:latest
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: demo-app
        # # - secretRef:
        # #     name: demo-app
      imagePullSecrets:
      - name: regcred
