apiVersion: v1
kind: Service
metadata:
  name: demo-app
  annotations:
    "consul.hashicorp.com/service-sync": "true"
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8081
  sessionAffinity: None
  type: ClusterIP